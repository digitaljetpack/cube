{
  "name": "cube-viewer",
  "version": "1.0.0",
  "private": true,
  "description": "3D cube viewer with vectors, projections, azimuth/elevation arcs using Three.js + local OrbitControls.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon --watch public --watch server.js server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "three": "^0.160.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=18"
  }
}
